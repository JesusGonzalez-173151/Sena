<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="luna.joel.sena">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sena">
        <activity android:name=".TarjetaRegistro"></activity>
        <activity android:name=".MotivoActivity" />
        <activity android:name=".CrearCuenta" />
        <activity android:name=".Egresos" />
        <activity android:name=".Ingresos" />
        <activity android:name=".MainActivity" />
        <activity android:name=".EmailLogin" />
        <activity android:name=".SeleccionarLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>